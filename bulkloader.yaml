# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Signal
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: access_count
      external_name: access_count
      # Type: Integer Stats: 41067 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: client
      external_name: client
      # Type: String Stats: 1237 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 41067 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_email_notified
      external_name: last_email_notified
      # Type: Date/Time Stats: 454 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: modified
      external_name: modified
      # Type: Date/Time Stats: 41067 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: modified_by
      external_name: modified_by
      # Type: User Stats: 5 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: notification_setting
      external_name: notification_setting
      # Type: String Stats: 782 properties of this type in this kind.

    - property: notify_triggered
      external_name: notify_triggered
      # Type: Date/Time Stats: 524 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: receivers
      external_name: receivers
      # Type: Blob Stats: 41067 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: sender
      external_name: sender
      # Type: String Stats: 41067 properties of this type in this kind.

    - property: subject
      external_name: subject
      # Type: Text Stats: 41067 properties of this type in this kind.
      import_transform: db.Text

    - property: token
      external_name: token
      # Type: String Stats: 41067 properties of this type in this kind.

    - property: tz_offset
      external_name: tz_offset
      # Type: Integer Stats: 41035 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


